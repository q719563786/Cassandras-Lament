# 远见 v1.1 使用说明

1. 双击 `YuanJian.exe`。首次打开会出现三步教程，依次告诉你行动首页、行为输入和后台监控怎么用。
2. 平时只看“行动首页”。第一条“最先做”是当前优先事项；每条按“建议、为什么、最迟时间、风险级别”排列。
3. 首页最多显示 3 条真正影响你的风险。风险用高、中、低中文显示，不要求你理解 L1—L4 或证据等级。
4. 自己发生了新情况，点击“告诉远见”，用一句话写事实或行为，再点“告诉远见并判断”。系统会在本机保存并直接返回建议和风险级别。
5. 只有需要核查时才点“查看原因”。新闻、因果链、反对证据和公开来源默认折叠，不需要你每天阅读。
6. 没有高风险时会明确显示“目前没有需要你处理的高风险”。如果公开来源全部失效，系统会显示“监控覆盖不足”，不会误说没有消息。
7. “提醒”可以只看未读、单条标为已读、全部标为已读，并打开关联风险。
8. “设置”中有后台监控、我的资料、使用教程、通知中心、情报后台和知识库。点击“重新查看使用教程”可再次打开三步引导。
9. 情报后台默认只显示摘要；只有主动点击后才显示原始新闻、来源或技术错误。
10. 外部 AI 默认关闭。只允许发送公开标题、摘要、网址、时间和通用类别；私人利益、Obsidian 原文、地址、医疗、债务和账户不得外发。
11. “知识库”只读索引 Obsidian，不修改原文。断网时历史缓存、本地研判、利益映射和预测账本仍可使用。
12. **关闭窗口就是安全退出**（停止后台监控、释放端口、退出进程）。想让它继续在后台盯，就别关窗口；托盘菜单里有“显示”“运行认知”“暂停监控”和“退出远见”。只有点击公开证据链接时才会打开系统浏览器。
13. “设置”里可以自己调两个数：**备份保留份数**（1–30，份数越多占磁盘越多）和**远程 AI 每日上限**（填 0 表示关闭远程，只用本机研判）。改完立刻生效，不用重启。
14. “设置”里的**立即备份**按钮会真的做一份备份，并告诉你文件名和大小 —— 换版本或改数据前可以先点一下。

运行数据默认位于 `%LOCALAPPDATA%\YuanJian`。想放到别的盘（例如 D 盘），在 `YuanJian.exe` 旁边放一个名为 `data-dir.txt` 的文本文件，里面只写目标目录即可，例如 `D:\远见\数据`。改完重新打开程序就生效，原来的数据需要自己搬过去。

不要把这个目录、数据库、备份或密钥文件上传 GitHub 或发给别人。

如果新版本暂时不符合预期，可以安全退出后回到上一个版本 —— 安装目录旁边保留了上一版目录（`程序-旧-<时间戳>`），改名换回去即可。但请注意：**v1.0 之后的版本都改过数据库结构**（新增过列与触发器），所以旧版本能否正常打开新库**没有验证过**，回退前请先备份数据目录。
