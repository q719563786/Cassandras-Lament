# 变更记录

面向使用者的重要变更。每个版本的验收数据（测试结果、产物哈希、未覆盖范围）见 `docs/releases/`。

## v1.5.2 · 2026-09-23

**这一版的主题是「把"安静地坏掉"摆到台面上」。** 修三个用户看不见的故障（设置页会白屏、诊断页两态永不显示、
暂停原因被说错），给采集加一道时间预算，并把远程状态改为按「是否启用」门控。四处改动都不改业务判定。

### 设置页：`/api/interests` 失败会把整页渲染打断

「设置」页顶部并发拉六份数据，每份都以 `.catch(() => null)` 降级为 `null`——一个区块失败不该拖垮整页。
但「个人利益」区块的渲染函数对 `interests` 这个 `const` 重新赋值：接口失败（`null`）时抛
`Assignment to constant variable`，`render` 中断，**整页白屏**。根因**两处**：

- 列表渲染 `paintInterests`：`if (!interests) interests = {...}` —— 对 `const` 赋值。
- 新增表单提交：`interests.objects = []` —— 在 `null` 上设属性，二次崩溃。

修法：一律改用**局部变量**读取，绝不重赋值/改写 `const`；并把「接口失败（`null`）」与
「成功但空（`{}`）」两种占位**分文案**（失败态提示「稍后重试」），让该区块「明确表现」。

### 诊断页：远程研判「暂停 / 已回退本机」两态终于显示

「远程研判到底还活着吗」此前**无人能看见**：后端没有这四个字段，前端状态条恒为 `undefined`，
于是远程失效、程序**静默降级到本机研判**时，界面看起来一切正常。补齐后端四字段
`ai_paused` / `ai_pause_reason` / `ai_fallback_local` / `ai_fallback_reason`——只用**既有状态**
（`paused_auth` 行与 `remote_error_fallback_local`），**不新增状态机**。原因一律用**人话**，
且**认证失败优先于熔断**（前者要用户动作，后者只需等对端恢复）。

### 诊断页：暂停原因不再硬说成「密钥失效」

暂停可能是**熔断**（连续失败），也可能是**密钥失效**。此前前端把缺省原因写成「密钥无效，请到设置重新填写」——
熔断时这句话是**错的**，会把用户推去改一个没问题的密钥。改为中性提示「远程研判已暂停，请到设置查看原因」，
真正的分支由后端 `ai_pause_reason` 给出。

### 远程状态按「是否启用」门控：主动关闭不再被报成故障

若用户**主动关掉**远程研判，库里通常还留着关闭前那批 `paused_auth` 残留行；不门控就会把「你自己关的」
报成「已暂停（密钥失效）」，把人推去修一个**他故意关掉**的东西。修法：这两个状态**只在 `ai_enabled` 为真时**
才计算，关闭时一律不报故障（前端「未启用（默认关闭）」那一态才是真相）。读数失败保持默认、**不猜**
（猜成"已暂停"比不说更糟）。

### 采集：每轮加 120 秒时间预算

`refresh_due_sources` 此前会给**所有**到期源发抓取，对端大面积 TLS/连接超时时一轮可跑到 ≈35 分钟、
**独占**后台单线程循环。引入 `COLLECT_PASS_BUDGET_SECONDS = 120`：每轮**先看表再动手**，到点就不再
**开始**新源（至少开一个），剩余到期源留到下一轮（调度器 30s 后触发）。常态（≈88s）仍一次抓完、
不增延迟；病态最坏从 ≈35min 压到 ≈2min。

> ⚠ 预算是**按「源」切**的：一个源一旦开始，它的**条目处理仍会整批跑完**（单条目 ≈2.7s）。
> 即单轮仍可能较长。已列 v1.5.3「按条目预算」候选。

### 测试：补齐两处盲区

- `WITHOUT ROWID` 表的估算退化路径此前无任何用例走到，本次补测（含估算来源标记）。
- 为设置页那处 `const` 重赋值补一条**树内变异对照**（把修复改回 bug → 必须变红），保证修复真的被守住。

## v1.5.1 · 2026-09-23

**这一版的主题是「让失败留下痕迹」。** v1.5.0 修好了「判级」，这一版修的是另一类更隐蔽的问题：
**程序正在悄悄失败，而界面上一切看起来正常**。共七个提交（十项改动），全部不改业务判定
（唯一一处改判定的是首页条数，见下）。

### 抓取层：源返回 gzip 会让它静默停采

`urllib` **不会**自动解压。源若回 `Content-Encoding: gzip`，程序拿到的是压缩字节，
解析**必然**失败，被记成 `parse_error`，`consecutive_failures` 一路涨到该源被禁用 ——
全程**没有一句显式报错**（日志里只有"解析失败"），用户只会觉得"这个源怎么不更新了"。
这是代价最高的一类失败：源没了，但没人知道。

- 两条抓取路径都显式声明 `Accept-Encoding: identity`（先请它别压）。
- 仍做**防御性解压**：声明了 gzip 就必须解开，解不开**明确报错**（`bad_encoding` 数据不完整 /
  `too_large` 疑似炸弹）；只靠魔数判断的模糊情况允许按原样处理，但**记 warning 不吞现场**。
- 逐块解压并把解压后体积卡在 **4×上限（20MB）**，防 gzip 炸弹；超限按 `too_large` 记账，**不静默截断**。
- 顺带补上 `fetch_json` 缺的 **SSL 证书降级回退**（`fetch_bytes` 早就有）——此前证书有问题的站点
  「走 RSS 能通、走 JSON 直接失败」，同一个站点两种命运。

### 远程付费：一天可能悄悄发出约 2400 次调用，而预算写的是 2000

日预算此前只统计 `finished_at IS NOT NULL` 的作业，而**失败重试的那条分支不写这个字段**
⇒ **重试完全不计入预算**。远程服务长时间故障时，一天可发约 **2400 次**真实付费调用。三处修：

- **账目改为按次记账**：新计数器在成功 / 格式错 / 鉴权失败 / 重试 / 超限兜底**五个出口**各计一次，
  跨重启不丢；老库在计数器尚未写入前退回原行数口径（刻意保留的兼容路径）。
- **默认上限 2000 → 200**。2000 是照着**免费**的 Agnes 端点定的（免费版限的是每分钟请求数，
  没有每日配额），一旦用户把端点换成**付费** provider，同一个数字就从"白给"变成"一天几千次真实扣费"。
  **默认值只改默认，不覆盖用户已显式存过的值** —— 真库实测其配置显式含 `daily_budget=2000`
  且用的是免费端点，所以本机用户改动前后**都是 2000**。免费额度宽松的用户自己在设置页调高即可。
- **新增连续失败熔断**：同一 provider 连续 5 次非鉴权失败后，把它的排队作业统一冻结
  （复用既有暂停机制，不另起一套状态）。并加了**半开探测** —— 每 15 分钟放行**一条**去试，
  成功即整批解冻。没有这条，熔断等于把 provider **永久封死**：作业全被冻结进不了到期集合，
  而"成功一次即解除"又要求先有一次成功，两条加起来就是**永远等不到那一次成功**。

### 静默失败治理（主线）

统一原则：把"悄无声息地失败"改成"**留下痕迹的失败**"，但**不改变任何业务判定**。

- **关机不再可能悄悄花钱**：关闭时的队列清理此前是 `except: pass`。清理失败时，排队中的作业会**活过关机**
  并在下次启动后继续发出去 —— 而该函数自己的文档字符串承诺"确保不会有新的 API 请求发出"。
  现在失败记 warning，并把排队作业冻结为 `paused_shutdown`（不在到期集合内、跨重启不自动拾起）。
- **作废预测的孤儿引用**：预测被作废时清除个人影响上的关联字段。选择"**写时清除**"而不是"渲染时过滤"——
  孤儿引用是**落库事实**，渲染时过滤会让同一行在不同页面上解释不同。异常不再 `pass`，
  失败计入审计并从日志可见。
- **定时任务失败记完整堆栈**（此前只记异常类型，代码注释自承"等于没有现场"）。
  状态表只多一个 `error_location="函数名:行号"`，**不进用户界面**。
- 另有四处静默失败变为可观测（控制流不变）：认知自动确认、认知重排队列、影响分类惩罚、
  数据库同步模式回退。
- 顺手修掉一个**测试盲区**：两条关机用例原先写在非 `TestCase` 类体里，`unittest` **从未收集**它们 ——
  它们从未跑过，却让"全绿"看起来成立。

### 清理层：时间形状护栏统一（纯防御，零行为变化）

清理判据是**按字符串字典序**比时间，而写入端存在多套时间形状（`Z` / `+00:00` / 空格分隔 / 空串）。
位置 10 处 `' '(0x20) < 'T'(0x54)`，所以"空格分隔的今天"会被判成比"T 形状的截止值"更旧 ——
**该留的被删、该删的删不掉**。原先只有两层有护栏，现照搬到余下四层：非标准形状一律比较得 `NULL`
⇒ **永不删**（保守方向：不确定就不删）。真库只读复核：**加护栏前后五层删行数完全相同、对称差 0**
（A307/B527/C2854/D0/E147/F316）——今天它并不落在任何清理列上，本次仅为消除隐患。
代价：将来真有异常形状的旧行会"焊死"在表里（只多占空间），这是可接受的。

### 交付环境里，「单表 > 500MB」这条护栏其实一直是坏的

该护栏用 SQLite 的 `dbstat` 扩展算单表体积，而**打包进交付件的 `sqlite3.dll` 没有编译 `dbstat`**
⇒ 在用户手上**静默失效**（不崩、不报错、永不触发）。**这是既有问题、非本版引入**（代码可追到 2026-09-14）。

先实测再定方案（真库 1.05GB / 26 表）：`dbstat` 与 `sqlite_dbpage` 都不可用；逐表行数 × 前 N 行平均行宽
偏差 **0.32~5.55 倍**且方向不定（弃用）；全表平均行宽准但要 **0.8~16.6 秒**（弃用）；
**采用「沿 rowid 均匀开窗采样」：偏差 0.97~1.11 倍、全库约 3.7 秒**。行宽取**字节**而非字符
（按字符会低估中日韩文本六成以上），并乘保守系数 1.15（实测系统性低估约 12%）——**宁可早触发**
（清理本身幂等且安全），也不要因低估而让单表无限膨胀。

退化与"连估算都失败"两种情况**都记 warning**，返回值与诊断面板都**如实标注来源**
（`dbstat`=精确 / `estimate`=估算）——一个不知道自己"是估算值"的护栏等于没有护栏。
**主护栏（库文件 vs 2048MB）一个字节未动。**

### 首页「最多 3 条」：文档一直这么承诺，代码不是

`/api/risk-dashboard` 此前传 `limit=50`，而 README 与使用说明都写「首页只呈现最多 3 条个人事项」。
**改代码让文档成立**（改回 3），并补一条端到端断言钉住这条契约 —— 此前正是因为无人守，才会一路漂到 50。

### 前端：监听器泄漏导致「点一次、提交多次」

今日页每 45 秒自刷，而卡片动作绑定每次都往**持久的**容器上**再挂一遍** click 监听 ⇒ 叠加，
于是确认按钮会重复渲染 + 重复提交。事件簇页有同款写法，一并修（统一为"先按引用移除再添加"）。
测试是**真跑 JS**（连续渲染三次后一次点击只触发一次），并有变异对照证明探针有牙。
另修：来源徽标的 `title` 属性此前未转义。

**诊断页新增「远程研判真实状态」状态条**（用大白话，不出现内部术语）：正常 / 有限流退避 /
暂时连不上已改用本机研判（附末次失败原因）/ 已暂停（密钥失效），并始终显示「今天用了 X / Y 次」。
此前鉴权失效后程序会全程本机跑，而界面看起来一切正常 —— 现在看得见。

### 图标改版（14 枚）

全部按「**版画式印章**」语言重画：对称、几何、像刻在蜡上的印而非插画（眼在环中＝观察、
四芒星落于环＝标定、衔尾环＝循环、闭合之眼＝离线……）。新增 `ic_atlas`（古式浑天环）——
此前「今日远见」与「全球态势」共用同一枚图标，侧栏两个一级导航图样相同。
**键名一律不变，调用方零改动**；全部 `currentColor` 描边、无外部资源。

> 版权说明：密教模拟器／司辰之书的图标是 **Weather Factory 的版权素材，不可搬入本仓库**。
> 此处只借鉴其**视觉语言**（版画式单色线刻、对称几何），符号为**自绘原创**。

### 其他记录

- **清理了两行死代码**（gzip 重构后无人引用的常量与不可达分支）。值得一提的是一条**教训**：
  这两行曾被误判为"工作区被并发写入损坏"而回滚过一次 —— 那是**误判**（把同事已验证的正确清理
  当成了故障）。看到工作区变化应先查来源；回滚是破坏性操作，而 `git diff` 为空只说明"与提交一致"、
  不等于"这是个好状态"。
- `signals` 的时间字段在接口不带时曾落**空串**，已改为回退"收到时刻"。
  **未回写用户库里已有的那 1 行**（不越权改用户数据）。
- `audit_log` 中并存两套时间助手，本次未统一（记录，见验收文档「已知局限」）。
- 版本号升到 1.5.1 的理由：以上改动都进了打包产物，同一个版本号对应两个不同构建会让验证记录失去意义。

## v1.5.0 · 2026-09-20

**把「判定」接回方法论，并新增一层全球态势图层。**

### 为什么要动判定（这是本次最重要的一处）

用户原先的质问是「同一个 L4 一直重复弹」。查下来通知链路确实有两个 bug（见 v1.4.2），
但**修好之后他仍然觉得 L4 太多**——那才是真问题，而且不在通知层，在**判级层**。

`impacts.py` 的分数是四项加权：`证据 0.3125 + 重要度 0.3125 + 涉及面 0.25 + 紧急度 0.125`。
问题在于这四项**对同一类事件几乎是固定值**：

- **重要度取自"利益对象"的预设分值**，用户 7 个利益对象全是 3 分 ⇒ 这一项**恒为 0.6，从不变化**；
- 涉及面是写死的类目对照表；紧急度是对 AI 给的时间窗做关键词匹配；
- 于是只有"来源数"随事件变化 —— **同一类事件的分数几乎恒定 `0.68125`，而门槛是 0.68**，
  只高 **0.00125**。

结论：**它判断的不是「这件事重不重要」，而是「这件事属于哪一类」**。一天二十几个不同事件
被判成「立即行动」，内容各异但分量一模一样 —— 用户看到的"重复"就是这么来的。

**修法：用已经算好、却没参与定级的结构化产物定级。** 程序本来就在算
`gyw.power_structure`（发文层级 + **按最低纳入层判定**的执行阻力 `delay_risk`，
语义正是"改革威胁执行者利益就会停""发文方级别高不代表落得下去"），也落了库、也渲染在事件详情页上，
**唯独没进分数**。现在：

- **L4 增加一结构性闸门**：必须 `delay_risk ∈ {高, 中}` 或命中风险信号才放行；
  不满足则降为 **L3**，降级原因写进 `components_json`（`l4_downgraded_by` / `l4_gate_rule` / `l4_gate_delay_risk`）可回溯。
- **`importance` 不再恒定**：改为「利益侧预设 × 事件侧结构强度」。
- **未知不罚**：结构信号缺失时按中性处理，**不因信息缺失降分**；但缺失也**进不了 L4** ——
  信息不足不进最高档，也不假装它不重要。全库 52.7% 的事件该信号为"未知"，若按惩罚处理会让 L3 从 16% 塌到 6.8%。
- **E1 封顶 L3、三个阈值 0.45/0.60/0.68 都没动**（前者是对外承诺，后者是照真实分布挑的）。

**效果（真实库实测）**：最近 24 小时 L4 从 **68 条降到 6 条**、涉及事件从 22 个降到 **4 个**；
最近 7 天 L4 137 → 77；**每日汇总（L2）7 天内 8437 → 4515**。
被压回「需要关注」的是例行公告与远处战事（央行逆回购、LPR 维持不变、大摩维持评级、
黄金 ETF 吸金、尼泊尔泥石流、胡塞袭沙特……）。

### 两处收尾（否则上面这处会开倒车）

- **远程 AI 研判没有 `power_structure`** —— 而远程研判是用户花额度换来的升级版。
  不补算的话，新闸门会把它们**系统性降为 L3**，等于"花了钱的分析反而进不了最高档"。
  改为在计算路径上**用本机规则引擎读时补算**（不改写研判原文、不改 AI 契约、零成本、无新增调用），
  并在 `components_json` 里标 `structure_source` 以示来源。
- **存量 119,331 行 `personal_impacts` 一次性回填**（迁移 v7），否则用户打开程序看到的还是旧档位。
  写成可重入迁移、**执行前先备份**、只更新 `alert_level` 与 `components_json` 两列。
  **已知局限（口径选择，已记录）**：历史行会因后续报道使证据等级上升而被重算，
  故 9/8–9/16 那几天的 L4 从个位数升到几十条（只影响历史回看）；`personal_relevance` 缺失的
  7,346 行按最相关 1.0 处理，属偏乐观假设。

### 密教主题（外观层，只换外观不改行为）

新增 `static/css/theme-mystique.css`（逐字节保真的交付件），追加在 `views.css` 之后加载以覆盖原样式。
13 枚图标换 10 枚（雷达→眼、齿轮→钥匙、输入→羽毛笔、电源→门、离线→闭合的眼…），
emoji 换几何符号（`❖ ⊙ ◈ ◇ △ ▸ ↑ ↓`）。全部本机资源、无字体外链、无内联样式。

顺带修好两个**长期失效**的既有缺陷（主题 CSS 生效即解决）：`--primary` / `--success` /
`--success-bg` 三个变量被引用却从未定义；`.ac-direction` 的方向标签因原选择器多一层 `dir-`
前缀而**从上线至今从未着色**。

### 全球态势图层（新增）

把公开的全球事件源接成一张可自助查询的地图，新增视图「**全球态势**」（`#/atlas`）：
Canvas 2D 自绘世界轮廓（随包投放的 Natural Earth 公有领域数据，无地图库、无在线瓦片、无 CDN）、
滚轮缩放 + 拖拽平移、图层开关、时间窗（24h/72h/7d）、点选详情、**数据新鲜度显示**。

- 三个无密钥官方源：**USGS 地震 / NASA EONET 自然事件 / GDACS 灾害告警**（均 T1）。
  新增 `geojson` 抓取类型（映射全部由 `config_json` 驱动，解析器零硬编码）。
- **这批数据与研判链物理隔离**：独立表 `situation_events`（迁移 v8），
  **不进 `external_items`、不进聚类、不进 AI 研判、不产生通知**。
  理由：USGS 一天两三百条地震，灌进研判链会同时炸掉成本与信噪比。
- 量级**不跨图层比较**（地震是震级、火山是 VEI、风暴是风速、野火/洪水/干旱是上游指数）。
- 保留期 **30 天**，接进既有 `RetentionService`（F 层），不另起清理系统。

### 其他

- **GDELT 源**：实测 575 次运行中 `unreachable` 占 77.7%、`rate_limited` 17.6%、成功 4.2% 且产出为 0
  （TLS 握手超时 + 对方限流）。加**失败退避**（连续失败按 `15/30/60/120/240/360` 分钟递增，成功即归零）
  并把预置周期 15→120 分钟。**不自动停用**——网络恢复时它会自己回来，程序也不该擅自关用户的源。
- **AARO 采集结论（记录）**：美国国防部 AARO（全领域异常解析办公室）**不可采集**。
  实测 `aaro.mil` 及 `defense.gov` / `war.gov` 等对该网络全部返回 Akamai 403（机房出口 IP 被拒），
  不经该出口时域名在 **DNS 层即被拒**；且 AARO 本身**没有 RSS/JSON/API**。故这一类只能走第三方源。
- 「天外实体」知识块的触发词收紧：真库 79,800 个事件簇上，**旧词表命中 630 条、其中真与 UAP 相关的 0 条
  （100% 误命中）**；新词表命中 0、误命中 0，并以正负例做了召回验证
  （误命中样本如"义乌开始卖 UFO 了"、"广州蓝皮书"、"不明飞行物击中船只"（实为导弹）、"失联**外籍**人员"）。
  新增利益对象「前沿与天外」。

## v1.4.2 · 2026-09-19

**修好通知链路：Windows 通知中心不再反复刷出同一条没有内容的「远见 / 新通知」。**

两个根因，各有独立证据：

- **弹窗内容永远是空的**。给 Windows 发通知的那段 PowerShell 脚本，此前把标题与正文当成
  命令行尾参传入（`powershell.exe -Command <脚本> <标题> <正文>`），而 `-Command` **不会**
  把尾参绑进 `$args`（实测 `$args.Count = 0`）——尾参反被 PowerShell 当命令解析，直接报错退出
  （退出码非 0）。于是 Python 侧每次弹窗都抛 `RuntimeError`、记录降级成 `local_only`，
  Windows 拿到的 toast 标题与正文都是空串，通知中心只能显示系统占位文案「新通知」。
  真实库里 `notification_log` 的 **96 条 L4 全部 `local_only` + `error_message='RuntimeError'`**，
  就是这条链路从未成功过的指纹；`wpndatabase.db` 里该应用的 toast payload 全是
  `<text id="1"></text><text id="2"></text>`，与时间戳逐条吻合。
  **修法**：标题/正文改走**环境变量**（`YUANJIAN_TOAST_TITLE` / `YUANJIAN_TOAST_BODY`），
  `argv` 末尾不再有尾参。toast 文案**一字未改** —— 通用文案是有意的隐私设计，
  系统通知里不出现事件内容、类别或利益名。
- **同一件事反复弹**。去重此前只认「证据哈希变没变」，而事件簇每吸收一条新消息
  （哪怕只是同一篇通稿的转载）哈希就变，于是 **6 小时节流形同虚设**：一条同文转载就能让
  同一条事件再弹一次。

现在的系统通知纪律（README 与实现一致）：

- **同一事件 6 小时硬下限**：距上一条通知不足 6 小时，**只有告警等级升高**才允许再弹；
- **6–24 小时**：需要**实质性提升**（证据等级上升 / 独立来源变多 / 行动窗口跨档）才提前提醒；
- 超过 24 小时允许重新提醒一次；
- **每天最多 6 条系统弹窗**（滚动 24 小时配额）。配额用尽时**不再弹窗，但仍写进本地通知中心**
  （状态 `unread`）—— **不丢信息、只少弹窗**；配额用尽不是发送失败，`error_message` 保持为空。

升级到 1.4.2 的理由与 1.4.1 相同：以上改动都进了打包产物，同一个版本号对应两个不同构建
会让验证记录失去意义。

## v1.4.1 · 2026-09-19

**对外改名：Foresight → Cassandra's Lament（中国对外名「卡珊德拉的哀歌」）。**

只改对外可见的部分，**程序本身一点没动**：

- GitHub 仓库名 `q719563786/Foresight` → `q719563786/Cassandras-Lament`，简介同时给出两个名字（GitHub 不允许空格与撇号，所以地址里写成 `Cassandras-Lament`）。
- 代码里的两个 GitHub 地址（`update_check.DEFAULT_ENDPOINT` 与 `RELEASES_PAGE`）、`PRIVACY.md` 里写明的那条请求地址、README 首页标题随之更新。
- **程序界面、包名、目录、数据目录、快捷方式一律不动** —— 窗口标题仍是「远见 · 外部认知大脑」，对话里也仍叫远见。
- 旧地址依然可用（实测 `api.github.com/repos/q719563786/Foresight/releases/latest` 返回 200 并跳到新仓库），所以**已经装好的旧版本不会因为改名而失联**。

版本号升到 1.4.1 的理由：这次改动进了打包产物（那两个地址在 exe 里），同一个版本号对应两个不同的构建会让验证记录失去意义。

## v1.4 · 2026-09-18

> 依据《远见 v1.4 整改任务书》（三轮设计审计 + 对真实数据库的只读核查）。这一轮的目标只有一个：
> 让「预测 → 结算 → 校准 → 修正」这条闭环**第一次真正跑起来**，并清掉审计确认的失真源。
>
> 起点（只读核查，未写入）：账本 **8,607 条预测、0 条结算**；逾期未结算 1,691 条；
> 生成速度约 **391 条/天**；`base_rate` 全为 NULL；`confirmed_by` 全是 `'unknown'`；
> 卡片正文里 `observable_signals` 非空的 **0 条**；L4 的 145 条里 128 条挤在同一个分值 0.675。
> 按 S0 → S3 四批落地，每批独立可停。

**S0 · 让闭环通电**

- **批量结算（R-01）**。开环不是使用者的疏忽：结算被设计成"逐条三次交互"，而生成速度是
  391 条/天 —— 跟上需要每天 1,174 次交互。**这是吞吐量问题，不是纪律问题。**
  现在可以按「类别 + 到期区间 + 状态」一次结算一批，**一次事务**完成，任一条失败整批回滚。
  界面上先预览"将结算 N 条、结果 X、影响到期区间 Y"，因为 `resolutions` 不可变、**批量结算不可撤销**。
  - **默认结果必须是无害的。** 这是本轮最不可妥协的一处：批量默认用 `indeterminate`，
    **绝不能用 `not_occurred`**。用后者等于凭空给成百上千条命题盖上"没发生"的断言，
    而这些断言不是观察、是默认值 —— 它们会直接进入 Brier 与命中率/误报率的分子分母。
    与上一轮刚修掉的"机器填的概率混进人类命中率"是同一个病，只是更严重。
  - 新增 `forecasts.resolved_by`（`user` / `batch` / `unknown`），与 `confirmed_by` 同构：
    批量写出来的「无法判定」是**没来得及看**，逐条判定的才是**看了但判不了**。
- **到期自动归档（R-02）**。到期不结算的条目此前无限累积（每月约 1.2 万条），首页横幅只是提示、
  不是机制。现在有一条**默认关闭**的规则："到期满 N 天后自动记为无法判定并归档"（N 默认 30，
  范围 7–180）。默认关闭不是保守，是**账本不可变**：自动写进去的归档删不掉也改不了，
  不能让默认值替用户做这个决定。
- **校准面板不再把机制问题说成时间问题（R-03）**。旧文案写的是"已结算的预测还太少，
  等远见多跑几轮再看校准"—— 而实测那 8,607 条历史命题的 `observable_signals` 全为空，
  校准准入把它当硬门槛，**它们即使补结算也永远进不了校准**。现在面板如实分开说，
  并把"已排除"的构成写出来（缺可观测事实 / 缺基准率 / 含不可结算表述），**三类之和等于排除总数**。

**S1 · 让分数是在量"人"**

- **校准指标按来源拆分（R-04）**。`hit_rate` / `false_positive_rate` / 整体 `brier` / `by_category`
  此前全都跨来源聚合，而文案写的是"**你确认过的预测里**，真的发生了的比例"。
  CHANGELOG v1.1 自己写过为什么必须拆，被点名的三个分数一个都没拆。现在主指标只统计
  `confirmed_by='user'`；机器与历史不明的那部分**分列**给出，不合并、也不隐藏。
  基准率同理**只采人工二元结算** —— 把自动条目算进去，base_rate 会变成"机器填的概率的自我回声"
  （自动条目的概率本就由 base_rate 生成，结算后又回流成 base_rate）。
  代价是诚实的：人工样本长期不足 5 条时该类别的基准率保持 NULL，新预测因此进不了校准；
  界面把样本构成（本类别 N 条、其中人工 M 条）写出来，而不是只丢一句"样本不足"。
- **关注度分数去掉与来源数共线的项（R-05）**。`base_score` 里 `evidence` 与 `confidence`
  都是"独立域名数"的单调函数，**合计权重 0.45** —— 同一个变量计两次。v1.2 已把**概率**与来源数
  解耦，但驱动首页排序、通知、以及"能否被自动写进账本"的分数没动，而这恰好是用户每天看到的东西。
  现在移除 `confidence`（它仍留在 `components` 里供回溯），其余四项按比例放大。
  阈值随之重定，**依据是真库 118,379 条 `components_json` 重算出来的分布**
  （见 `build-artifacts/v14_probe_*.py`）：三个新阈值都落在该分布的空档里，
  没有任何一个切开密集取值簇。四档占比从「L2 占 91.7%」变成
  **L1 39.8% / L2 42.4% / L3 15.6% / L4 2.2%**。

**S2 · 把证伪性闸从"形状"变成"实质"**

- **删掉两项恒真检查（R-06）**。闸门原先查四件事，其中两件结构上不可能失败：日期查的是
  `title + criteria` 里的 `\d{4}-\d{2}-\d{2}`，而 title 由 `_candidate` 自己写成 `f"截至{end_date}：…"`；
  判定依据查 criteria 里有没有"核验/官方/公告"，而 criteria 是同函数硬编码的文本。
  **那一段在校验它自己刚写下的字。** 删掉，而不是改成校验外部输入 —— 本闸只该拦"命题内容"。
- **禁用词不再误伤新闻标题（R-07）**。禁用词检查跑在 `title + criteria` 上，而 title 内嵌了新闻原标题：
  一条标题写了"降息**可能影响**楼市"的新闻会让整条候选被拒。含糊的是**我们的命题**，
  不是被报道的那句话。现在只检查生成的部分，另把新闻原标题单独落 `source_headline`。
- **可观测信号必须事件级特化（R-08，本批次最关键）**。默认（本地）路径下 `observable_signals`
  永远是"事件类型 → 固定短语"的查表结果 —— 任何 policy 类事件都拿到同样那五条。
  后果：**闸门在默认路径下全部放行**，账本里装的是"分类平均命题"：形式可结算，实质不含本事件信息。
  现在要求至少一条信号含本事件特有实体（机构名/地名/数字），或与事件标题共享一段 ≥4 字的连续片段
  （"专有名词"那一档；取 4 字是为了避开"发布/公告/通知"这类任何事件都会出现的通用词）。
  不满足的候选**停在"待补充"，并在候选卡上直接标出原因**。
- **结算判据改为主信号 + 窗口期内首次观测（R-09）**。旧判据对最多 4 条信号取**或**
  （四条各 30% 独立，或运算后约 76%，命题天然偏"发生"），且"在该日期前发生"
  **包含窗口开始前的既成事实** —— 政策类模板信号里的"…发布配套实施细则"往往在事件被采集时
  已存在，命题从建立那天起就已成立，**零证伪风险**。现在指定一条主信号（其余只作辅助观察），
  并要求信号在 `window_start` 之后、`window_end` 之前**首次被观测到**；
  证据里已带既成事实标记的，判为"窗口开始时已成立"，不入账。

**S3 · 失真源与文案**

- **跨域同文检测（R-10）**。条目身份只按 `canonical_url` 去重，`content_hash` **算出来只存不用** ——
  同一篇通稿被 5 家门户全文转载 = 5 个不同 URL = 5 个独立域名 → 直接进 E2。
  README 声称"同域转载不冒充互证"，但它只防同域，而同一 URL 本来就被去重了，
  **真正最常见的形态（跨域通稿）没有防**。现在同一 `content_hash` 跨域名**只算 1 个独立声音**，
  并真正影响 E 级判定；事件详情页写明"本事件 N 个来源中 M 个为同文转载"。
  一个诚实的边界：v1.4 之前写入的事件簇没有 `source_domains` 这一列，迁移**不回填**
  （本仓一贯口径：不知道就是不知道），于是老簇会出现"独立声音 2 > 原始来源 1"这种
  自相矛盾。此时**不硬算** —— 两个数一起标成未知、界面不显示那一行，
  等该簇下次有新条目进来时重算恢复。这条是启动烟测在真实数据上发现的，
  已用 `test_legacy_cluster_without_source_domains_reports_unknown` 钉住。
- **趋势阈值不再用固定倍数（R-11）**。真库 14,315 个快照里 `rising` 占 48%
  （只看可判定的占 **73%**）—— 一个多数时间在报警的探测器等价于没有报警。根因是每轮要对
  最多 56 次比较各判一次"是否翻倍"，却按单次比较的尺度取固定倍数，**没有多重比较校正**。
  现在单次比较的显著性水平 = 20% / 本轮比较次数，阈值取 `期望 + z·√期望`；
  并新增健康指标 `rising` 占可判定快照的比例，**超过 20% 即视为阈值失效**，
  在诊断面板显示警示。
- **账本补不可删保护与创建时间（R-12）**。`forecasts` 此前一个触发器都没有 → 父行可删、
  子行删不掉，真库实测留下 **10,663 条（55%）永久孤儿**；父行被删还会让那条结算记录从
  `resolutions JOIN forecasts` 里整体消失（基准率与 `excluded_total` 双双静默变小，
  v1.2 那句"不静默缩小总数"的承诺对这类删除无效）。现在补 `no_delete`
  （**只加 delete，不加 update** —— `status` 的 `open → resolved` 流转是有意的），
  并给 `forecasts` 加 `created_at`（**历史行不回填**）；`resolutions` 冗余存
  `category` 与 `confirmed_by`，让基准率与来源拆分真正不依赖父行存活。
  随之而来的一处**行为变更**：`purge_garbage_forecasts` 从"删父行"改成"置 `void`" ——
  删除父行正是那 10,663 条孤儿子行的来源；与其让这条路径每次启动都抛 IntegrityError
  被静默吞掉，不如按账本本来的语义作废。
- **文案口径统一（R-14）**：首页空状态与引导语不再把"关注度档位"说成"风险"。
- **证据等级可达性写进界面（R-15）**：实测 E3/E4 从未出现，因为 `primary_source`
  从未在任何信息源上标记过 —— 四级体系实际只跑两级，最窄概率区间（E4 ±0.07）不可达。
  诊断面板现在显示等级分布，并明说"尚未标记任何官方来源"。**不替用户标记任何来源。**
- **生成量收敛（R-16）**：不再为 L1/L2 生成候选预测。真库 118,379 条个人影响里 L2 占 92%，
  而首页最多显示 3 条 —— 信噪比约 39,460 : 1；这些候选本来就不显示，却仍然生成、仍然占库、
  仍然参与通知节流。判断用**最终等级**（含风险信号上调），所以"上调到 L3"的候选照常生成。
- **机构名匹配的过配行为固化（R-17）**：`_LOCAL_ORG` 的中缀文法会把"本地区政府""区域管理局"
  也判成地方政府（方向保守，宁可判高）。加测试钉住当前行为，避免以后无意扩大。

**未做**：R-13（10,663 条孤儿版本行是否开受控删除路径）是**产品决策**，按任务书要求不执行。

**验收**

- 全量测试 **673 项，连跑三轮全绿**（`ResourceWarning` 视为错误）。
- 三层验证：内容级（解出 exe 里的 PYZ，30 个 v1.4 标识符 + 26 项静态资源全中）、
  启动烟测（无窗口真跑，26 项只读断言；**并据此发现上面那个历史列的边界并修掉**）、
  行为级（临时库走完整链路，含变异对照）。
- **15 条变异对照全部"有牙"**（`build-artifacts/v14_mutation_controls.py`）：逐条把本轮改动改坏，
  对应的断言必须变红。挑两条：把批量默认结果改成 `not_occurred` → "不污染校准"的断言变红；
  把 `confidence` 加回 `base_score` 并还原旧阈值 → "等级不再随来源数上升"的断言变红。
- 顺带说明：这也是本轮**发现两个探针缺陷**的方式。R-04 的变异最初打错了位置（主指标的来源拆分
  在 `calibration_summary` 的 SELECT 里，不在 `base_rate_for_category`）；R-14 的变异影响不到断言，
  因为测试用 `__file__` 定位到**真实仓库**的 src —— 只拷 `src/` 的变异树测不了读源码文本的断言。
  两处都已修正（变异脚本现在连 `tests/` 一起复制）。

## v1.3 · 2026-09-17

> 同一份设计审计的**第四、五波**。v1.2 把**校准链的地基**浇完了（命题可结算 → 概率有来源 →
> 门槛与真实性）；这一轮做的是**接出口**与**认识论**——把已经算出来但没人看得到的东西接上界面，
> 并把几处**方向反了**的机械规则扭正。

**一、最有方法论价值的东西此前没有任何出口**

系统为 `beneficiaries` / `cost_bearers`（谁获利、谁承担成本）做了全仓**最严的反幻觉校验**——
引用必须 ⊆ 证据包、无引用必须带 `[推断]` 前缀、矛盾直接报错——然后把结果丢在一个
**前端零引用**的字段里。同一份数据里还有一批同样的情况：

- `scenario_paths`（多路径推演）：后端挂在 `gyw.scenario_paths`，前端读 `j.scenario_paths`，
  且两边字段名完全不一致（后端 `label/path/probability:"60-70%"`，前端要 `path_type/description/probability:数字`）
  → **「多路径推演」面板从未渲染出来过**
- `gyw.interests`（② 利益方向槽位）：schema 里**根本没有这个键** → 永远显示「-」
- `power_structure` / `risk_signal_hit`：算了，**没有任何出口**

现在事件详情页有：② 利益方向（获利方 / 承担成本方，`[推断]` 与「有出处」分开标）、
① 权力结构判定（含**判读依据**，让用户看得见"凭什么这么判"）、⑦ 历史押韵（标明是模板类比）、
⑧ 风险信号（命中了哪些词）。`actors` 的语义也修正了——它此前装的是**来源域名**，
界面把「某网站」当成了「当事方」。

**顺带发现：这一页用到的 53 个 CSS 类在 CSS 里一个都没定义。**「接出口」就算接上，
用户看到的也是裸 HTML。已按既有设计令牌补齐（只加类，不动令牌值）。

**二、三处方向反了的机械规则**

- **「慷慨激昂」上调的是我方置信度**（`confidence += 0.08`）——而方法论说的是
  *「慷慨激昂 = 内心已感知风险」*，即**风险更高**，不是"我们判断得更准"。
  越慷慨激昂系统越自信，方向正好相反。现在改为**上调关注度等级**，并把命中词记入候选卡与账本。
- **`risk_boost` 从未参与任何运算**：用户看到「风险上调 +15%」，而那个数字只是拼在文本里的装饰。
  现在它按权重**真实折进概率中心**，并合计封顶（8 条模式可同时命中，不封顶就成了
  「关键词越多概率越高」——正是第二波刚拆掉的失真源）。
- **权力结构 markers 含裸「国家」二字**（且 `"市"` 命中"市场"、`"部"` 命中"部分"）
  → **执行阻力恒判「低」**。而方法论的核心恰恰是"改革威胁执行者利益就会停"——
  执行层阻力才是重点。现在改为「策展机构全称 + 地方政府文法」两条高精度通道，
  且 `delay_risk` 由**证据里出现的最低执行层**决定：国务院发文、要求各市水务局落实 → **高**。

**三、假设不再冒充事实**

九大知识库此前以「**必须用以下逻辑判断**，而非通用AI视角」的姿态进入**每一次**研判。
后果不是"AI 不够懂这套方法论"，而是**这个 app 在放大用户的模型，而不是检验它**——
与《登高望远》GYW-013 的方向相反。

现在：注入文本开头即声明**「候选假设，不是事实」**，并写明三条使用规则（证据优先、
证据冲突以证据为准、**不得为了让结论符合假设而裁剪证据**）；每个知识块都带
`state`（`unverified` / `supported_by_source`）与**「主要限制或反证方向」**——
用户知识库里本来就有这套状态机，应用侧此前把它丢了。
**天外实体侧移出通用注入**，只在与 UAP / 航天 / 解密相关的事件上按需注入。

**四、黑天鹅不再有数字概率**

多路径推演此前给三条路径分别写 `60-70% / 20-30% / 5-10%`。后两个数字是编的；
黑天鹅那个尤其不能有——**给它数字等于声称视线覆盖了视野之外**。
现在路径一概不给数字概率，并附一句说明为什么。

**五、L1–L4 是关注度，不是风险量级**

`score = 证据×.25 + 置信×.20 + **我在乎**×.25 + 领域相关×.20 + 紧迫×.10`——
里面有两项衡量的是"与我有多相关"，所以它算的是**关注度**。界面此前翻成
「高/中/低风险」，会被读成损失规模。文案改为「重点关注 / 需要关注 / 低优先」，
并**单独给出量级**：证据里有金额/数量就列出来（但不据此下"严重"的结论），
没有就写「**未量化**」——不编数字，也不用看起来严重的词替代没有的数字。

**六、势头不再被 72 小时切断**

同一件事的后续报道超过 72 小时会被判成新事件，各自进账本、各自算概率——
「势头」在数据层就断了。现在 72 小时之外**只对近重复**放行
（`score ≥ 0.85`，或有共同数字且 `score ≥ 0.70`），合并原因记为 `continued_subject`
落进 `event_cluster_items.merge_reason`，**可审计**；不是近重复的仍然不合并，超过 30 天一律不合并。

**七、集成时发现的第 4 次「字段静默丢失」**

证伪性闸在**入账那一刻**用 `observable_signals` 判断命题能不能结算，
可 `confirm_candidate` 根本没把它传给 `create_forecast` → **账本里没有"阈值或可观测事实"，
到期时命题实际上无法机械核验**。`base_rate` 同理：第二波加了字段，却从未落过库。
`alert_level` 也被硬编码成 `L3`。三处一并修复。

**为什么同一个形状出现了四次**（前三次：`confirmed_by`、`observable_signals`、`base_rate`）：
`_normalized_card` 是**重建 dict 的白名单**，不在返回表里的键会被静默丢掉。
**规则：给这个仓库加任何新字段，第一件事是查它有没有进这条链。**

**八、测试方式的改变（这一轮最该保留的东西）**

这个仓库此前对视图模块只做「源码文本包含某些字符串」的断言——而这**恰好漏掉了
v1.2 引入的一个会让整页崩掉的缺陷**：`renderGywSection(gyw)` 内部引用了不在其作用域的
变量 `j`，一调用就抛 `ReferenceError`，被 try/catch 吞掉后用户看到「加载失败」。
**文本断言全绿，页面全崩。**

现在 `tests/test_cluster_render.py` 把 `cluster.js` 拿到 node 里**真的跑一遍**，
并带**变异对照**：把带缺陷的那一版（`dc22a18`）用同一套夹具跑，必须复现
`ReferenceError` —— 证明探针有牙。另加一条机械护栏：详情页用到的每个 CSS 类
都必须在 CSS 里有定义（若有 53 个没定义就会红）。

## v1.2 · 2026-09-17

> 依据一份设计审计：**「远见认识《登高望远》里所有的词，但没做那件事」**——词汇表到位、认识论缺席。
> 审计称本节第一条为「最致命」：账本里装的命题**不可结算**，而账本的唯一用途是校准。
> 这组改动不动架构，只把**校准链的地基**浇完。

**一、命题从"会不会产生影响"改成"能不能被一条未来新闻判定"**

原先账本里的命题是「某事件**将在观察期内影响**某利益」+「判断该事件是否**产生实际影响**」——
缺对象、缺方向、缺阈值、缺判定人，**无法证伪**。无法证伪的命题进不了校准，
账本于是退化成自我确认。

现在每条命题必须四要素齐备：**对象 / 方向 / 阈值或可观测事实 / 判定依据**，
可观测事实取自研判已有的「领先指标」与「触发条件」。

**新增证伪性闸**：机械检查「有日期、有可观测条件、有判定依据、不含禁用虚词」。
禁用短语表（`产生实际影响`、`待补充`、`后续官方公告`、`视情况`……）的共同点是
**永远不会错，因此无法证伪**。不合格的候选**停在"待补充"，不进账本**。
**素材里没有可观测信号时不编造**——直接判为不可入账。

**二、概率不再等于"多少个网站转载了"**

原先概率与置信度由证据等级（E1–E4）直接给出，而 E 级由**独立域名数**决定 ——
量纲错误：E 测的是来源独立度，却被当成了事件发生概率。后果双向地坏：
**多家转载的平庸公告拿高概率进 L4**，而**真正重要的单源线索只能拿 0.25–0.55**。

现在拆成两件：
- **概率中心 = 基准率 + 信号调整**
- **证据等级只决定区间宽度**（E1 最宽 0.18，E4 最窄 0.07）——证据越弱，区间越宽

**基准率的来源是账本自己的历史结算记录**（同类别已结算预测的命中率）。
不用外部查表——**编一个数字比没有更坏**。样本少于 5 条时**如实报"样本不足"**，
不用任何默认值顶上；这类预测仍可产生候选，但**不参与校准**。

**三、L4 改为必须人工确认**

删除映射阶段的 L4 自动入场；自动确认收窄到只认 L3。
兑现 README「候选预测必须人工选择固定概率后，才能进入不可变预测账本」这条承诺 ——
此前 E1 被降到 L3 后会被自动确认，那次修复实际没有生效（**改档位不等于加闸**）。

**四、校准口径把两个数分开**

校准面板现在显示「已结算 N，其中 M 可用于校准，K 已排除」，
而不是一个被静默缩小的总数。**账本里已有条目一律不清理**（不可变承诺），
只做标记与排除。

**五、趋势加入采样偏差护栏**

趋势统计的是本系统自己生成的事件簇数量。源管理启停信息源会让事件数上升并被判为
「升温」——原先没有区分"世界变了"和"我这边采样变了"。现在会标明
「采样口径有变动，解读需谨慎」。

**六、集成时发现并修掉的四个缺陷**

同一个形状出现了三次：**一个字段默认值埋下的坑，在不同地方重复发生。**

- `observable_signals` 不在 `_normalized_card` 的白名单里、也不被渲染 —— 创建时闸能读到，
  **存库时被静默丢弃**，命题最关键的要素从未落盘。（v1.1 的 `confirmed_by` 栽过同样的坑。）
- 校准排除判据写成 `confirmed_by != 'unknown'` —— 而该字段 **DEFAULT 就是 `'unknown'`**，
  于是**任何新建预测都被误排除**，连「已结算总数」都被清零。
- `base_rate_for_category` 里有同一行过滤 —— **基准率机制把自己锁死**（样本永远为 0，
  永远拿不到基准率）。已改为按"是否结算且结果二元"取样，并注明理由。
- `row.get("base_rate")` —— `row` 是 `sqlite3.Row`，**没有 `.get()` 方法**，
  会抛 `AttributeError` 把整个校准面板打崩。已改用 `keys()` 判断列是否存在。

**七、分析失败不再伪装成"分析过了"**

`repair_judgment` 原先承诺「永不返回 None」：远程 AI 输出不合规时，它会把缺的字段
填上占位默认值（`observable_signals` 填「后续官方公告 / 执行进展通报」），
构造一份**保证通过校验**的研判 —— 然后照样入库、照样在界面上显示成
一份完整的「登高望远·六步分析」。**界面上看不出它其实没分析。**

现在研判带 `analysis_status` 三种取值：
- `real` —— provider 真的产出了分析
- `degraded` —— 走过**字段修补**（AI 没给出来的字段被默认值顶上了）
- `placeholder` —— 走过**最小兜底**（内容实质为空，只是为了让流程不中断）

并且：
- **非 `real` 的研判不得产生候选预测** —— 候选会进不可变账本并参与校准，
  不能建立在"其实没分析"的基础上
- **界面明确显示「本次未生成有效分析」**，不再让占位内容看起来像真结论
- 判定**由 `repair_judgment` 自己如实登记**，不靠事后嗅探内容里有没有"待补充"
  （那种字符串嗅探很脆弱）
- 实现上**没有加表列**：状态随 `JudgmentResult.to_dict()` 写进 `content_json`，
  因此不需要迁移、不碰 `judgments` 的不可变触发器；
  它也**不放进发给 AI 的结构化输出契约**（那是本机标记，不该由 AI 产出）

**八、打开首页不再改动账本**

`risk_dashboard()`（即 `GET /api/risk-dashboard`）原先会调 `auto_confirm_all()` ——
**一个读路径在写不可变账本**：刷新一次首页就改变账本状态，而且
「当时用户看到的是什么」无法复现，审计与回溯失去基准。该调用已删除。

写账本现在只发生在两条路径上：**周期认知任务**（按你的设定自动确认 L3）与
**你在界面上的显式确认**。删除读路径的写库**没有改变**「只有 L4 要人工」这个策略。

## v1.1 · 2026-09-16

**预测账本标明来源，E1 不再自动入账（重要）**

- **账本里每一行现在都标明它是怎么来的**：`你选的`（本人在界面选的概率）、`系统自动`
  （按 E2 及以上多源证据自动确认）、`早期不明`（v1.1 之前的历史行 —— 当时没有记录，
  **不假装知道**）。校准面板的账本表格新增「来源」一列。
  **为什么必须做**：账本是不可变的，里面的行如果分不出是人选的还是机器填的，
  那么命中率、误报率和 Brier 分数就混进了人类从未做过的预测，**这些分数就不再有意义**。
- **E1 单来源线索一律不自动进入账本。** 此前自动确认只按告警级别（L3/L4）筛选、
  **不看证据等级**，于是 v1.0 那次「E1 不得超过 L3」的修复**实际没有生效** ——
  E1 被降到 L3 之后照样被自动确认（真库中已有 1,081 条这类预测）。
  本次补上证据等级闸：只对 **E2/E3/E4** 自动确认；关联不到事件簇或证据等级为空的行，
  同样不自动写入。E1 必须由本人选择概率后手动确认。
  **教训写在代码注释里：改档位不等于加闸。** 用降级去阻止一件事，而下游的判断条件
  根本不看那个维度 —— 这是同一种病的第二种形态。
- **E2 及以上的自动确认保留**，没有一起关掉。

**设置现在真的能改（重要）**

- 修了四处「值存进去了，但不生效」的缺陷。它们表现不同、病根是一个：**设置被别处的旧值覆盖回去**。
  - **备份保留份数**：保存时永远沿用旧值、清理时用的是写死的常量 —— 这个设置**从来没能生效过**。
  - **「自动清理过期数据」开关**：关不掉（保存时被页面加载时的旧状态覆盖）。
  - **改备份目标时段会把备份开关悄悄关掉**：先开启每日备份、再改时段，开关被写回「关」，
    **备份从此不再运行**，而界面上看不出任何异常。这是四处里后果最严重的。
  - **「启用远程 AI」开关**：点了不保存，必须再点「保存」才生效，与旁边三个开关行为不一致。
- **新增两个可配置项**（设置页可改，**改完下一份备份/下一轮即生效，无需重启**）：
  - **备份保留份数**（1–30，默认 7）。调小可省磁盘；升级前的手工快照**永不删除**。
  - **远程 AI 每日上限**（默认 2000，可填 0 表示关闭远程；原先写死 100）。
    配套把开闸间隔从 1 小时放宽到 15 分钟、每轮额度从 10 提到 25。
- **「立即备份」按钮此前是空壳**：点了只弹一句「将在下个时段执行」，背后**没有任何后端接口**。
  现在真的会备份，并显示真实的备份文件名与体积；失败时显示真实原因而不是「服务内部错误」。
  **每日自动备份关着时，手动按钮照样可用**（升级或换版本前可以先手动备一份）。
- **保留天数输入框清空不再静默改小**：原先清空输入会被当成 60/180 写入，
  而保留天数变小意味着**更早删除数据**。现在会提示错误并保留原值，不提交。

**远程 AI 的用量与速率**

- 上面那次放宽配额之后，实测**撞了服务商的限流**：15 小时内 13% 的请求被拒（429），
  被拒的作业退避 15 分钟 —— **净吞吐反而被压住**，远低于设置允许的上限。
  原因是每轮一次排 20+ 个作业、连续不断地发出，瞬时速率超过了服务商的每分钟上限。
- 新增**请求速率闸**：两次请求之间至少间隔 5 秒（每分钟不超过 12 次，取上限的六成，
  留余量给重试与其它调用）。**本机研判不联网，不受影响。**
- **被限流的退避从 15 分钟改为 1/2/4 分钟。** 429 的含义不是「请求有问题」而是「发太快了」，
  长退避会让积压作业一起到期、下一轮又变成一次新的突发 —— 越等越糟。
  改后最迟第 4 次降级本机处理，约 7 分钟收敛（原先 105 分钟）。
- **每日上限与速率闸各管一件事**：一个管「一天多少次」，一个管「一分钟多少次」，
  两者独立、不可互相替代。这次的问题正是**只有每日上限、没有速率闸**。
- **诊断面板**在 AI 一栏显示「限流退避中 N」，N>0 时转为警示色 ——
  让你能直接看出「是不是调太快了」。同时显示当前速率闸的间隔。

### 隐私与数据完整性修复

**隐私边界修复（重要）**

- **外部 AI 不再接收任何个人数据。** 此前远程研判会把「用户主动记录的个人近况、
  利益对象与关系、最近若干条预测」连同请求一起发送给所配置的 AI 服务——这与
  `PRIVACY.md` 的三处明文承诺直接冲突（「利益对象与关系…预测历史」永不进入外部 AI；
  「外部 AI 的唯一输入」是 `build_public_bundle()` 生成的公开证据包；唯一的主动外发是
  「检查更新」按钮）。
- 现在远程请求**只包含公开证据包**，并有两道独立防线：队列的远程分支只做剥离、不再注入；
  两个 provider 共用同一个出口函数，该函数会主动移除个人上下文并校验体积上限。
  即使将来有人误把个人画像接回远程路径，也不会外发。
- **本机个性化不受影响**：影响映射与告警分级本来就在本机完成；本机路径仍可按隐私级别
  使用个人上下文（这正是「更强的个性化走本机模型」这条路要用的）。
- 远程提示词中要求 AI「结合用户近况（收入、工作、所在地）」写 `personal_action` 的段落
  已删除。远程产出的行动建议只基于事件本身。

**数据完整性修复**

- **不可变承诺此前并未真正强制。** 数据库触发器只有「禁止 UPDATE / DELETE」，
  而 SQLite 默认配置下 `INSERT OR REPLACE` 的冲突消解**不会触发删除触发器**，
  可以静默改写甚至抹掉一条「不可变」研判。已启用 `recursive_triggers` 修复。
- **`resolutions`（预测结算与 Brier 评分）此前完全没有触发器**，可任意改删。
  已补上，与 `judgments` / `forecast_versions` 一致。
- `forecasts.status` 的状态流转属有意可变（`open` → `resolved`），未加触发器，代码中已注明。
- 全仓排查确认：`src/` 中没有任何地方依赖旧行为，本次改动**不改变任何既有语义**。

**数据安全修复**

- **备份保留已静默退化：承诺 7 份，实际只剩 3 份。** 原因是备份文件的筛选用了过宽的
  文件名通配，把升级前留存的 `pre-*` 旧快照也算了进去，而它们在按名排序时排在日期备份之前，
  长期占住保留名额。同时诊断面板报告的「最近一次成功备份」指向一份 1.3 MB 的旧空壳，
  而不是真正最新的备份。已收紧为只认带日期戳的备份文件，`latest()` 与轮转共用同一筛选逻辑。
  **现存备份文件未做任何删除。**

**清理机制**

- **不再删掉你自己写的标注。** 此前清理过期簇明细时，会把你在界面上标过的
  「已忽略 / 误报」、静音设置、重要度覆盖，以及通知的「已读」标记一并删除——而这些
  正是学习回路要用的输入。现在带这些标注的行一律保留。
- 阈值触发的 6 小时节流改为独立时钟（存 `runtime_state`，每次尝试都推进）。
  此前节流读的是「只在真删时才写」的审计记录，导致没有可删数据时时钟永不前进、
  每个检查周期都完整重跑一遍全量清理。

**测试与覆盖率**

- 测试从 269 项增加到 **517 项**；总体覆盖率 **80.4% → 85.0%**。
- 低于 80% 的模块从 8 个降到 **6 个**。
- 本轮补上了三处此前**完全没有覆盖**的关键路径：外部 AI 请求的体积契约（此前只有
  一个 provider 校验，另一个实测曾发出近 1.9 万字符）、真实外发通道（含 DNS 解析后
  的地址复查）、以及不可变触发器的覆盖范围。
- 建立了一条测试纪律：**每条核心断言都必须先用"故意改坏实现"证明它会变红**，
  否则不算数。本轮据此发现并修复了若干"看起来在测、其实永远不会失败"的用例。

### 更早的加固轮次

**测试与覆盖率**

- 测试从 269 项增加到 **426 项**（+157）。
- 总体覆盖率 **80.4% → 84.1%**（可执行行 6948 → 7468，已执行 5589 → 6280）。
- `secret_store` **48.0% → 100%**（75/75）。此前它只有 37 行测试，DPAPI 的失败路径、
  非 Windows 分支、文件头校验、原子替换全部未覆盖。
- `http_api` **78.5% → 99.1%**（753/760）。任务书里的旧基线 71.1% 已被实测推翻。
  `retention` 94.0%。
- 剩余 7 行不覆盖并**如实记为取舍**：`http_api` 的 404-405（写错误响应时自身失败）、
  452（`length<=0` 纯防御分支）、461-462 / 478-479（对存活回环 socket 伪造 `OSError`）。
  这些只能靠白盒打桩够到，收益约 0.6% 且会被任何重构打碎，故不做。
- 低于 80% 的模块仍有 7 个（`judgment_validation` 63.2%、`external_sources` 64.5%、
  `remote_ai` 67.4%、`application` 68.5%、`radar_scheduler` 71.1%、`diagnostics` 73.1%、
  `runtime` 79.5%），**均为本轮范围外**，留待后续。

**工程度量**

- 新增 `tools/coverage_baseline.py`：零依赖的覆盖率基线工具（用标准库 trace，不引入 coverage 包）。

**速度**

- 补 5 条热路径索引，消除大表全表扫描。在你的真实数据库（894 MB）上实测：
  通知去重 55.7 ms → 0.01 ms、条目归属簇 26.5 ms → 0.01 ms、
  按来源查条目 110.4 ms → 0.23 ms、远程预算计数 36.8 ms → 0.23 ms。
  另有 1 条候选索引实测只快 1.3 倍，主动放弃。
  代价：13.7 MB 空间，首次启动多等约 4.6 秒（仅一次）。

**数据体积 · 分层清理**

- 清理机制从 2 层扩到 **5 层**，新增三层并全部可配置：
  - **C 层** `judgment_jobs`（作业流水，默认保留 7 天）：任务成功产出研判后这条流水即冗余。
  - **D 层** `external_runs`（采集运行记录，默认保留 90 天）。
  - **E 层** `trend_snapshots` 按窗口降采样：6h 留 7 天 / 24h 留 60 天 / 168h 留 365 天 /
    **720h 永久保留**。长窗口粒度本就粗，明细可删、趋势不丢。
- 新增**阈值触发**：库 >2 GB 或单表 >500 MB 时立即清理，两次清理间隔不少于 6 小时，
  人工触发可绕过。检查挂在 1 小时定时器上（`dbstat` 单次约 1.8 s，更频无意义）。
- **新增「结论明细保留天数」（默认 180 天，可设 30–730）**，过期事件簇的派生明细
  （个人利益影响、通知记录、研判任务、实体、簇成员）会被清理。
- **事件簇与研判永久保留**，这一点由数据库触发器强制执行、不可绕过；
  不可变预测账本同样不动。清理**不影响任何历史准确率统计**。

**数据体积 · 效果证据（实测）**

- 在你的真实库副本（937.1 MB）上跑一轮：删除 **51,736 行**，
  空闲页 **0 → 4,903 页 ≈ +19.98 MB 可复用空间**，而物理文件
  `937,140,224` 字节**完全不变**。
- **这是"清理确实生效"的唯一可见证据**：删掉的行变成了可复用页，文件因此不再为这批数据增长。
  只看文件大小会得出"什么都没发生"的错误结论——`auto_vacuum=0` 且本机制不执行 VACUUM。
- 第二次运行各层删除数为 0、空闲页增量为 0（幂等）；首轮耗时 8.2 秒。
- 测试套件里已加断言钉住这一点：清理后 `free_pages_after > free_pages_before`，
  且 `db_bytes_before == db_bytes_after`。

**数据体积 · 防冗余（F 组）**

- **F1 不做**：实测 `judgments` 表**本来就带** `UNIQUE(cluster_id, provider, evidence_hash)`，
  三元组重复组数实测为 **0**，该规则可防 0 行。改为在测试中钉死该约束，防止将来被误删。
- **F2**：写入前比对同簇最新一条研判的内容，完全相同则跳过。实测可拦 **2,200 条 ≈ 8.5 MB**。
- **F3**：单簇研判条数上限（默认 8，可设 0–1000，**0 = 关闭**）。
  实测 N=8 可防 1,193 条研判 + 1,731 条连带影响 = 8.2 MB；N=5/12/20 分别 14.0/5.0/2.5 MB。
- **F3 的产品代价已如实写入代码注释**：超限后该簇的研判**不再跟随证据更新**。
  N=8 只省 8.2 MB（相对 281 MB 判读体量约 3%），交换比并不好，因此做成可调、可关闭。

**数据体积 · 天花板（这是修正后的实测结论）**

- **此前本文件声称"把日增从约 26 MB 压到约 14 MB、收敛约 41% 的体积"，实测不支持，已更正。**
- 实测：一次性可回收约 **28 MB**（C 10.2 + E 1.1 + F2 8.5 + F3 8.2），
  稳态每日抑制约 **1.4 MB/日**（C 约 0.95 为最大项）。
- **日增约 26 MB → 约 24.6 MB，降幅约 5%。** 数字不好看，但这是机制能支撑的上限：
  删除类手段对"日增"量级几乎无影响，防冗余与稳态抑制合计仍是零头。
- `judgments` 实测 **280.8 MB**（占全库约 31%、占每日增量约一半）受「判读不可变」保护
  无法清理，**总体积无法收敛**。要让体积真正稳定只有两条路，都是产品决策：
  ① 允许对超龄研判做降采样（保留结论、丢弃正文，需放宽不可变约束）；② 接受增长靠磁盘兜底。
- **C 层的真正价值在稳态，不在那一次性 10.2 MB**：`judgment_jobs` 以约 3,384 行/日累积，
  C 层上线后每天清掉约 0.95 MB，一年约 340 MB 的持续抑制。

**数据体积 · A 层判据修正**

- A 层原判据 `published_at < cutoff` 对约 **80% 的行永远为假**，等于这些行永久删不掉：
  - `published_at IS NULL` 的 **77,231 行**（占 96,613 行的 79.9%）——SQL 中 NULL 比较结果为
    NULL，永不为真；
  - **537 行**存的是 RSS 原样的 RFC 2822（如 `Thu, 06 Aug 2026 13:34:05 GMT`）——
    首字符 `'T'`(0x54) > `'2'`(0x32)，字典序恒大于 ISO 截止值。
- 判据改为 `COALESCE(CASE WHEN published_at LIKE '____-__-__T%' THEN published_at END,
  first_seen_at) < ?`：**发布日期有效（ISO 形状）时优先用它，NULL 或非 ISO 形状时退化用
  `first_seen_at`**。
- **不解析 RFC 2822 回写、也不回填 NULL**——那是伪造发布日期；`first_seen_at`
  （首次看到该条目的时间）才是诚实的兜底，且实测 96,613 行零空缺。
- 入库侧**无需改动**：`external_sources.py` 早已有 `normalize_published_at()`，
  三条解析路径都在调用，新数据不再产生这两种形态。
- **实测今天多删 0 行**（表内 `first_seen_at` 只跨 39 天，未达 60 天阈值），
  旧判据与新判据在真实数据上都删 304 行。**这是纯未来防护**：不改的话，
  约 2026-10-05 起这张 55.7 MB 的表会开始只进不出。
- 代价如实记录：新判据用不上 `idx_external_items_published`，退化为全表扫描，
  实测 97.0 ms → 107.8 ms（+11%）。若该索引已建立，则是 0.3 ms → 约 122 ms。
  两者差距虽大，但清理每日仅一次，绝对代价约 0.12 秒，故选择保留简单判据。
- 顺带说明：**"归一化历史行即可恢复索引"是错的**。库中 80% 的行 `published_at IS NULL`，
  永远要走兜底，`COALESCE` 仍在，索引照样用不上。要拿回索引只有一条路——把判据结果
  **物化成列**（如 `retention_at`）并为其建索引，代价是要改写入路径。本轮不做。

**部署提示：正在运行的安装包落后于源码**

- 排查中发现真实库里 `database.py` 声明的 **5 条热路径索引一条都不存在**
  （`idx_external_items_source`、`idx_external_items_published`、
  `idx_notification_log_cluster_created`、`idx_judgment_jobs_finished`、
  `idx_event_cluster_items_item`）。
- 这不是代码缺陷：`application.py` 在数据库已存在时**每次启动都会调用
  `database.initialize()`**，而这些索引是 `CREATE INDEX IF NOT EXISTS` 无条件创建。
  唯一解释是**已安装的程序包早于引入这些索引的提交**，那些语句从未在真库上执行过。
- 影响：那几条热路径至今仍是全表扫描；且本轮全部改进（分层清理、路由重构、
  A 层修正、补测）在当前安装包上**均未生效**。**需要重新打包并安装**才会生效；
  首次启动会一次性补建索引（约 5 秒、约 14 MB）。

**清理**

- 移除 `/api/dashboard` 接口。它界面从未调用、无测试覆盖，却会把全部预测正文读出来
  再在 Python 里筛选，实测返回 **17.5 MB**；功能与界面在用的
  `/api/risk-dashboard`（141 KB）重叠，因此整体删除，含随之失效的 `SignalsService.high_alerts()`。

**结构重构**

- `http_api.py` 的 4 个 `do_*` 方法里原有 **110 个 if/elif 分支、49 个 `/api` 路由**，
  改为 **61 条声明式路由表**（GET 29 / POST 26 / PUT 4 / DELETE 2，列表顺序即优先级）。
  `do_GET` 9 行 / `do_POST` 15 行 / `do_PUT` 11 行 / `do_DELETE` 11 行。
  路由表在 `create_server` 时自检 endpoint 是否存在，写错名字直接报错而不是静默 404。
  安全性未降级：5 条安全响应头、令牌的定时安全比较全部保留。
- `judgments.py`（1247 行）按职责拆为 5 个模块：门面 61 行 +
  `judgment_models` / `judgment_bundle` / `judgment_validation` / `judgment_local`。
  拆分为 AST 零改写切片，**函数体一个字符未动**，公共 API 从原路径导入零丢失。

**健壮性**

- 超大请求体（>64 KiB）此前是"不读就直接拒绝"。改为**先有界排空（上限 1 MiB）再响应 400**，
  并给排空加 5 秒读超时。
- **这笔交换如实记录**：修复前该路径根本不读、不会阻塞；修复后，若对端声明长度远大于实发长度，
  工作线程会**最多阻塞 5 秒**。判断是成本路径在本应用不可达（只绑 `127.0.0.1`、令牌鉴权、
  客户端只有本机窗口），故保留。
- 曾观察到测试套件满载时该请求的 400 响应偶尔丢失（`RemoteDisconnected`）。
  **本机无法确定性复现**，该症状的修复为 best-effort，**不声称"已修复"**。
  可确定性钉住的是"有界性"这一半，已加用例与变异对照。

**文档修正**

- 修正文档与实现不符：`retention.py` 曾声称有「趋势降采样」而代码里并无实现——
  **本轮已把该功能真正实现（E 层），文档与实现现已一致。**
- 新增 `build-artifacts/CONTRACT-cdef.md`（不入库）作为交付契约，
  并修正设计文档中三处经实测推翻的论断：F1 是空操作、E 层回收数被时间格式低估、
  C 层一次性回收量被高估。

## v1.0 · 2026-09-13

首个带完整验收记录的正式版本。验收数据见 [`docs/releases/YuanJian-v1.0-verification.md`](docs/releases/YuanJian-v1.0-verification.md)。

**安全边界修正**

- E1（单一来源线索）现在被强制限制在 L3 以内。此前 `_alert_level()` 只按分数分档，E1 可以算到 L4；而 L4 候选会被 `map_judgment()` 自动确认，等于未经人工选择概率就写入不可变预测账本，同时从待确认列表消失。这修正了代码与 `README.md`、`PRIVACY.md` 声明之间的偏差。

**隐私**

- 公开源码中不再包含本机绝对路径。公开历史已改写并重建仓库，旧提交哈希不再可用。
- `PRIVACY.md` 明确「本仓库是公开仓库」为既定前提，并补充禁止本机路径、用户名、主机名入库。
- 新增「会话令牌的边界」，写明该令牌防的是本机以外与浏览器跨源，不防本机其他程序。

**安全加固**

- 所有响应统一附带 `Content-Security-Policy`（含 `object-src` / `base-uri` / `form-action` / `frame-ancestors`）、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、`Cross-Origin-Opener-Policy`、`Cross-Origin-Resource-Policy`。
- 会话令牌改用定时安全比较，并在前端读到后立即从地址栏抹除。
- 隐私闸门 `tools/privacy_scan.py` 修正了真实漏过一次的盲点（只匹配反斜杠形式的绝对路径），新增 `--committed` 模式，发布检查变成一条命令。

**性能与健壮性**

- 聚类的文本特征提取加入缓存，不再对同一簇重复分词；候选簇查询收窄到实际使用的列。
- 调度器下次到期改从任务结束时刻起算，修复任务超时后连续补跑的问题。
- 任务失败除异常类型外同时记录清洗并脱敏后的消息，便于现场排障。
- 新增滚动日志文件（`%LOCALAPPDATA%\YuanJian\logs`）；此前打包后无控制台，日志无落点。

**工程流程**

- 新增 CI（`.github/workflows/tests.yml`）：push 与 PR 时运行完整测试套件与隐私闸门。
- 新增依赖清单 `requirements.txt` / `requirements-build.txt`，构建脚本改为读取它们，版本只有一处真相。
- 新增 `.gitattributes` 统一行尾。
- 测试从 207 项增加到 220 项。

## v0.9 · 2026-08-12

行动引导界面：三步首次教程、行动首页只呈现最多 3 条个人风险、「告诉远见」一句话输入。验收数据见 [`docs/releases/YuanJian-v0.9-verification.md`](docs/releases/YuanJian-v0.9-verification.md)。

## v0.8 · 2026-08-12

风险驾驶舱。验收数据见 [`docs/releases/YuanJian-v0.8-verification.md`](docs/releases/YuanJian-v0.8-verification.md)。

## v0.7 · 2026-08-12

界面重做。验收数据见 [`docs/releases/YuanJian-v0.7-verification.md`](docs/releases/YuanJian-v0.7-verification.md)。

## v0.6 · 2026-08-11

桌面外壳与 pywebview 原生窗口。验收数据见 [`docs/releases/YuanJian-v0.6-verification.md`](docs/releases/YuanJian-v0.6-verification.md)。

## v0.5 及更早

早期数据库 schema 迁移与本地研判骨架，无独立验收记录。schema 迁移版本见 `tools/migrate_private_v05.py`。
