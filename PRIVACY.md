# 隐私规则

## 仓库定位

本仓库是公开仓库，只承载源码、测试和文档。公开可见是既定前提：任何新增文件在提交前都按“所有人可见”的标准审查，不假设仓库会转私有。运行数据、私人数据库、Obsidian 原文、备份和密钥一律不进仓库，也不通过仓库同步。

## 永不进入公开源码或外部 AI

- 数据库、日志、缓存、备份、运行状态和 DPAPI 密钥文件。
- 精确地址、完整生日、家庭关系、医疗记录、债务、账户和私人联系方式。
- Obsidian 原文、私人关注规则、利益对象与关系、个人影响、反馈和预测历史。
- 本机绝对路径、会话令牌、API 密钥和第三方 Cookie。

## 外部 AI 的唯一输入

经过 `build_public_bundle()` 生成的公开证据包：事件编号、公开标题和摘要、来源编号、域名、HTTP(S)网址、发布时间、E1—E4和通用类别。最多8个来源、12,000字符。报道内容始终标记为不可信数据，不能改变系统指令或提供者地址。

外部 AI 默认关闭。非密钥设置写入私人 SQLite；密钥只写 `%LOCALAPPDATA%\YuanJian\secrets\ai-token.dpapi`，由 Windows 当前用户 DPAPI 加密。

## 会话令牌的边界

界面与后台之间用一次性随机令牌保护（`secrets.token_urlsafe(32)`，每次启动重新生成），服务只监听 `127.0.0.1`，令牌通过 `X-YuanJian-Token` 请求头传递，前端读到后立即从地址栏抹掉。

需要明确的是：这个令牌防的是**本机以外**的访问与浏览器跨源请求，**不防本机其他程序**。令牌会写入 `%LOCALAPPDATA%\YuanJian\runtime\runtime.json` 供托盘与命令行复用，因此同一用户下的任何本机进程都能读到它。

由此得出的实际约束：不要在这台机器上运行来路不明的程序，也不要把运行目录或 `runtime.json` 分享出去。这是架构决定的边界，不是配置问题。

## 唯一的外发请求：检查更新

程序平时不主动外发任何数据。**唯一的例外是设置页的「检查更新」按钮**，而且只在你点击时触发：

- 请求地址固定为 `https://api.github.com/repos/q719563786/Cassandras-Lament/releases/latest`（项目对外名为 Cassandra's Lament／卡珊德拉的哀歌，内部代号「远见」；仓库改名后旧地址 `q719563786/Foresight` 由 GitHub 自动跳转，此处的地址已改为新名）
- 请求头只有固定的 `Accept` 与 `User-Agent`，**不携带版本号、路径、主机名或任何使用信息**
- 只读取公开发布的版本号用于比较，不下载、不上报、不轮询
- 断网、超时或返回异常时安静降级为"暂时查不到"，不会弹出错误，也不会谎报有新版本

除此之外，采集公开信息源是本程序的正常功能（读取公开 RSS / 网页 / 公开接口），不涉及你个人的数据。

## 发布闸门

交付包和公开源码不得包含 `.db`、`.sqlite*`、`.log`、`.env`、`.bak`、缓存、备份、密钥或运行状态，也不得包含本机绝对路径、Windows 用户名或主机名。发布前必须分别运行 `tools/privacy_scan.py`。GitHub 只用于公开源码分发，不是私人数据同步工具；计划改变仓库可见性或上传运行数据前，必须先取得明确确认。
